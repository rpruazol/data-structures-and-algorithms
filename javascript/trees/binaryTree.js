'use strict';

const {Node} = require('./node');

class BinaryTree {
  constructor(value=null){
    this.root = new Node(value);
  }
  preOrder(){
    // preorder traversal - travel down the left side of the binary tree console.log the value as you traverse
  }
  inOrder(){
    //  
  }
}