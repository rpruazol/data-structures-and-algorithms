
const {LinkedList} = require('../linked-list/linkedlist');

class HashTable {
  constructor()

  hash(){
    // this function will look at a key and "evaluate" it.  will return the location in the array index where we will store the value.  evaluate in this case will 
  }
}